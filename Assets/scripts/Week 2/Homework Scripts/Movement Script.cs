using UnityEngine;

public class MovementScript : MonoBehaviour
{
    //public Vector3 DirtBossMovement;
    //public GameObject Dirtboss;
    //public Vector3 DirtBossScale;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Dirtboss.transform.position += DirtBossMovement * Time.deltaTime;

        //Dirtboss.transform.localScale += DirtBossScale * Time.deltaTime;

    }
}
